# COAE-Forge backend deps.
# Most of these are already present in the `ai` conda env; pins reflect that env
# so `pip install -r` is a no-op there and reproducible elsewhere (CPU torch).

fastapi>=0.115,<0.120
uvicorn>=0.47,<0.48
pydantic>=2.13,<3
pydantic-settings>=2.14,<3
python-multipart>=0.0.29

torch==2.4.0
torchvision==0.19.0
opacus==1.4.0
transformers==4.51.3
scikit-learn>=1.8,<2

numpy>=2.0
pandas>=2.0
pillow>=11
matplotlib>=3.9

pytest>=8
httpx>=0.27   # for FastAPI TestClient
